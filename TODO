0. Add config.tests and scripts

1. if has Qt module(s), then include qglobal.h in xx_global.h. Otherwise, use os.h, compiler.h, cpu.h


2. deploy.pri

3. version 0.x.x error

4. make deb for all platforms. BUILDROOT=build/projname. MOC_DIR=$$BUILDROOT/.moc
POST_LINK=copy to bin
DLLDESTDIR

5. use qglobal.h if available

export BUILD_DIR
$$(BUILD_DIR)/.obj/$PROJECT/
copy common.pri to BUILD_DIR

dll's rpath(not only exe's)
